import{requireAdmin}from"../../../utils/admin";import{db}from"../../../utils/db";export default defineEventHandler(async event=>{await requireAdmin(event);const q=String(getQuery(event).search||"");return{users:await db.user.findMany({where:q?{OR:[{name:{contains:q}},{email:{contains:q}},{phone:{contains:q}}]}:{},select:{id:true,name:true,email:true,phone:true,role:true,status:true,createdAt:true,_count:{select:{whatsappSessions:true,conversations:true}}},orderBy:{createdAt:"desc"},take:100})}});

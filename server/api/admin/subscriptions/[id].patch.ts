import{z}from"zod";import{requireAdmin}from"../../../utils/admin";import{db}from"../../../utils/db";const schema=z.object({status:z.enum(["ACTIVE","PENDING","EXPIRED","CANCELLED"]).optional(),planId:z.string().optional(),endsAt:z.coerce.date().optional()});export default defineEventHandler(async event=>{const admin=await requireAdmin(event),id=getRouterParam(event,"id")||"",p=schema.safeParse(await readBody(event));if(!p.success)throw createError({statusCode:422});const subscription=await db.subscription.update({where:{id},data:p.data});await db.auditLog.create({data:{userId:String(admin.sub),action:"admin.subscription.update",entity:"Subscription",entityId:id,metadata:{...p.data,endsAt:p.data.endsAt?.toISOString()}}});return{subscription}});

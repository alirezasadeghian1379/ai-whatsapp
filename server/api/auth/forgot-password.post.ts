import{createHash,randomBytes}from"node:crypto";import{z}from"zod";import{sendPasswordResetEmail}from"../../services/mail";import{db}from"../../utils/db";const schema=z.object({email:z.string().email().toLowerCase()});export default defineEventHandler(async event=>{const p=schema.safeParse(await readBody(event));if(!p.success)throw createError({statusCode:422,statusMessage:"ایمیل معتبر نیست."});const user=await db.user.findUnique({where:{email:p.data.email}});if(!user)return{success:true};const token=randomBytes(32).toString("base64url"),tokenHash=createHash("sha256").update(token).digest("hex");await db.passwordResetToken.deleteMany({where:{userId:user.id,usedAt:null}});await db.passwordResetToken.create({data:{userId:user.id,tokenHash,expiresAt:new Date(Date.now()+30*60_000)}});const resetUrl=`${useRuntimeConfig().appUrl.replace(/\/$/,"")}/reset-password?token=${encodeURIComponent(token)}`;try{await sendPasswordResetEmail(user.email,user.name,resetUrl)}catch(error){await db.passwordResetToken.deleteMany({where:{tokenHash}});throw error}return{success:true}});
